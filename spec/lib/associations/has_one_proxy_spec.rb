require 'spec_helper'

describe ApiResource::Associations::HasOneProxy do
  # No need to test assign because it is tested by the
  # belongs to proxy tests
  context '#read_foreign_key'

  context '#write_foreign_key'

end